# conf/config.yaml
defaults:
  - llm: default
  - agent: demo
  - benchmark: default
  - _self_  # Allows variables to be defined at the top of this file

hydra:
  run:
    dir: ../../logs/debug

# You can define some top-level or default parameters here
project_name: "limira-agent"
debug_dir: "../../logs/debug"
